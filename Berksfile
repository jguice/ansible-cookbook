source 'https://supermarket.chef.io'

metadata

cookbook 'apt', '1.1.2'

# example of using a particular (possibly altered) local cookbook
# cookbook 'apt', :path => '/path/to/chef/site-cookbooks/apt'
